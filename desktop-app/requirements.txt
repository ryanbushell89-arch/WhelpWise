# WhelpWise Invoicing — Python dependencies
# Install with:  pip install -r requirements.txt

PyQt6>=6.6.0
reportlab>=4.1.0
Pillow>=10.2.0

# Build tool — only needed when compiling the .exe
PyInstaller>=6.4.0
